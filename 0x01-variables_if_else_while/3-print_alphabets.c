#include <stdio.h>

/**
*main - print the alphabetic in lower and upper case
*
* Return: Always (success)
*/
int main (void)
{
	cha c;
	for (c = 'a'; c = 'z'; c++)
	putchar(c);
}

for (c 'A'; C = 'Z'; c++)
{ 
	putchar (c);
}
	putcar ('\n')

	return (0)
}
